# Shared packages

Reusable code shared by multiple apps goes here.

Examples:

```txt
packages/
  ui/
  config/
  database/
  ai/
  utils/
```

Keep project-specific code inside `apps/<project>` unless it is genuinely reusable.
